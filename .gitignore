node_modules/
android/
ios/
*.apk
*.aab
.DS_Store
.gradle/
build/
.idea/
*.iml
local.properties

# Локальные секреты разработчика (keystore, пароли). НИКОГДА не коммитить.
.local-secrets/
*.jks
*.keystore

# Генерится в CI (Inject build info шаг). Локально лежит как stub.
www/build-info.js
